# =========================
# Docker 镜像设置
# =========================
# 先用占位镜像名；真正发布后改成实际镜像，例如：
# ghcr.io/rocoer7/shellcrash-feishu-bot:latest
BOT_IMAGE=ghcr.io/rocoer7/shellcrash-feishu-bot:latest
CONTAINER_NAME=shellcrash-feishu-bot

# =========================
# 飞书应用凭证
# =========================
FEISHU_APP_ID=
FEISHU_APP_SECRET=

# =========================
# ShellCrash 路由器 SSH
# =========================
ROUTER_HOST=192.168.31.1
ROUTER_PORT=22
ROUTER_USER=root
ROUTER_PASSWORD=

# 如果不用密码，改用 SSH 密钥：
# 1. 把私钥放到当前目录 ./keys/xxx
# 2. 设置 SSH_KEY_PATH=/app/keys/xxx
# 3. ROUTER_PASSWORD 可留空
SSH_KEY_PATH=

# =========================
# 飞书用户/会话白名单
# =========================
# 初次测试可以临时设为 true；正式使用建议 false。
ALLOW_ALL_USERS=false

# 用 /whoami 获取自己的 sender_id(open_id)，填到这里；多个用英文逗号分隔。
ALLOWED_OPEN_IDS=

# 可选：限制只允许某些会话使用；多个 chat_id 用英文逗号分隔。留空表示不限制会话。
ALLOWED_CHAT_IDS=

# =========================
# 其他
# =========================
MAX_REPLY_CHARS=3200
LOG_LEVEL=INFO
